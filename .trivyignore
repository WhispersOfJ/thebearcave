# Trivy exceptions for the active eight-service stack only.
# Review each entry whenever the corresponding image is refreshed.

# Seerr upstream image currently carries these unfixed dependency findings.
CVE-2026-33937 # seerr upstream image dependency; remove after image rebuild
CVE-2026-59873 # seerr upstream image dependency; remove after image rebuild

# Unpackerr stable image is awaiting an upstream Alpine rebuild.
CVE-2026-31789 # unpackerr upstream base image; remove after rebuild
