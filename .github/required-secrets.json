{
  "description": "GitHub Actions secrets that workflows in this repo may reference. This is the source of truth: the Secret Manifest Guard workflow fails when a workflow uses a secret not declared here, or when a declared env_var is missing from .env.template. Keep this file, .env.template, and the workflows in sync.",
  "secrets": [
    {
      "name": "GITHUB_TOKEN",
      "implicit": true,
      "reason": "Auto-provided by GitHub for every run; requires no repo configuration. Included so explicit secrets.GITHUB_TOKEN references are recognized."
    },
    {
      "name": "RELEASE_PLEASE_TOKEN",
      "env_var": "RELEASE_PLEASE_TOKEN",
      "required": false,
      "reason": "release-please opens the release PR with this token so validate.yml runs on it (PRs opened with GITHUB_TOKEN are skipped by GitHub's recursion guard). Without it the workflow falls back to GITHUB_TOKEN and release PRs ship without CI. Sync the .env value with scripts/setup.sh --sync-github-secrets."
    },
    {
      "name": "DISCORD_WEBHOOK_URL",
      "env_var": "DISCORD_WEBHOOK_URL",
      "required": false,
      "reason": "Discord webhook for cert-expiry-check and disk-cleanup workflow alerts. Optional — workflows skip Discord alerts when unset."
    },
    {
      "name": "LIDARR_API_KEY",
      "env_var": "LIDARR_API_KEY",
      "required": false,
      "reason": "Lidarr API key for stack integrations."
    },
    {
      "name": "READARR_API_KEY",
      "env_var": "READARR_API_KEY",
      "required": false,
      "reason": "Readarr API key for stack integrations."
    }
  ]
}
