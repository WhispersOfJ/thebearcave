# stack-tui window rule — sourced by the session sway config via `include`.
# The waybar custom/stack module (services/bash-functions/waybar/) toggles
# this window on the scratchpad; matching on the alacritty --class app_id
# keeps the rule independent of the XWayland class.
for_window [app_id="stack_tui"] floating enable
